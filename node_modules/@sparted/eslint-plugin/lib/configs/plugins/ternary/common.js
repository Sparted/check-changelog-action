'use strict';


const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  rules: {
    '@sparted/ternary/no-dupe': [ERROR],
    '@sparted/ternary/no-unreachable': [ERROR, {
      allowDuplicateOrConditions: false,
    }],
    // Conflict with unicorn/no-nested-ternary
    // OFF as we don't want to nest ternary
    '@sparted/ternary/nesting': [OFF, {
      test: false, // Default
      consequent: true, // Default
      alternate: true, // Default
      depth: 1, // Default
    }],
  },
};
