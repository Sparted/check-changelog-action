'use strict';

const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  parserOptions: {
    ecmaVersion: 12,
    ecmaFeatures: {
      jsx: true,
    },
  },
  settings: {
    'react-native/style-sheet-object-names': ['StyleSheet'], // Default
  },
  rules: {
    '@sparted/react-native/no-unused-styles': [ERROR],
    // OFF as it is a nightmare to maintain for little benefit
    '@sparted/react-native/sort-styles': [OFF, 'asc', {
      ignoreClassNames: false, // Default
      ignoreStyleProperties: false, // Default
    }],
    '@sparted/react-native/split-platform-components': [ERROR],
    '@sparted/react-native/no-inline-styles': [ERROR],
    '@sparted/react-native/no-color-literals': [ERROR],
    '@sparted/react-native/no-raw-text': [ERROR, {
      skip: [], // Default
    }],
    '@sparted/react-native/no-single-element-style-arrays': [ERROR],

    /* ----- Reset base config ----- */
  },
};
