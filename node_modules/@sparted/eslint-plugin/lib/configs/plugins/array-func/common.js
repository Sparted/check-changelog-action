/* eslint-disable @sparted/unicorn/prevent-abbreviations -- This is the name of the plugin */

'use strict';


const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  rules: {
    '@sparted/array-func/from-map': [ERROR],
    '@sparted/array-func/no-unnecessary-this-arg': [ERROR],
    // OFF as it conflicts with the rule unicorn/prefer-spread
    '@sparted/array-func/prefer-array-from': [OFF],
    '@sparted/array-func/avoid-reverse': [ERROR],
    '@sparted/array-func/prefer-flat-map': [ERROR],
    '@sparted/array-func/prefer-flat': [ERROR],
  },
};

/* eslint-enable @sparted/unicorn/prevent-abbreviations */
