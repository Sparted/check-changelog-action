/* eslint-disable @sparted/unicorn/prevent-abbreviations -- False positive */

'use strict';

const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


const functionNames = ['t']; // Default


module.exports = {
  rules: {
    '@sparted/react-i18n/no-dynamic-translation-keys': [ERROR, {
      functionNames,
    }],
    '@sparted/react-i18n/no-missing-interpolation-keys': [ERROR, {
      functionNames,
      prefix: '{{', // Default
      suffix: '}}', // Default
    }],
  },
};

/* eslint-enable @sparted/unicorn/prevent-abbreviations */
