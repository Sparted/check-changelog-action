'use strict';


const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  rules: {
    '@sparted/toplevel/no-toplevel-var': [ERROR],
    '@sparted/toplevel/no-toplevel-let': [ERROR],
    // OFF as it is too hard to respect it
    // TODO: Should ideally be ERROR
    '@sparted/toplevel/no-toplevel-side-effect': [OFF],
  },
};
