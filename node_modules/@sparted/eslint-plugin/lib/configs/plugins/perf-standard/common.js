'use strict';


const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  rules: {
    '@sparted/perf-standard/no-instanceof-guard': [ERROR],
    '@sparted/perf-standard/no-self-in-constructor': [ERROR],
    // OFF as it is too restrictive
    '@sparted/perf-standard/check-function-inline': [OFF],
    // OFF as we use array iterators
    '@sparted/perf-standard/no-array-iterators': [OFF],
  },
};
