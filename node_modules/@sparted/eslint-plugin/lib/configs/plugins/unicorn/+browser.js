'use strict';


const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  rules: {
    '@sparted/unicorn/prefer-add-event-listener': [ERROR, {
      excludedPackages: ['koa', 'sax'], // Default
    }],
    '@sparted/unicorn/prefer-dataset': [ERROR],
    '@sparted/unicorn/prefer-event-key': [ERROR],
    '@sparted/unicorn/prefer-modern-dom-apis': [ERROR],
    '@sparted/unicorn/prefer-node-append': [ERROR],
    '@sparted/unicorn/prefer-node-remove': [ERROR],
    '@sparted/unicorn/prefer-query-selector': [ERROR],
  },
};
