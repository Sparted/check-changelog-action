'use strict';


const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  settings: {
    'import/resolver': {
      node: {},
    },
  },
  rules: {
    /* ----- Helpful warnings ----- */
    // OFF as the plugin doesn't support CommonJS export
    '@sparted/import/no-unused-modules': [OFF],

    /* ----- Module systems ----- */
    // OFF as we commonjs in node context
    '@sparted/import/no-commonjs': [OFF, {
      allowRequire: false, // Default
      allowConditionalRequire: true, // Default
      allowPrimitiveModules: false, // Default
    }],
    // OFF as we use node module in node context
    '@sparted/import/no-nodejs-modules': [OFF, {
      allow: [], // Default
    }],
  },
};
