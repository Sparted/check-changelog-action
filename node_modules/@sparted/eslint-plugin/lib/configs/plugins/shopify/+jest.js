'use strict';


const OFF = 'off';
const WARN = 'warn';
const ERROR = 'error';


module.exports = {
  rules: {
    '@sparted/shopify/jest/no-all-mocks-methods': [ERROR],
    // OFF as we use jest snapshots
    '@sparted/shopify/jest/no-snapshots': [OFF],
  },
};
